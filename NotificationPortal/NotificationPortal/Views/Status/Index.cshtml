@model IEnumerable<NotificationPortal.ViewModels.StatusVM>

@{
    ViewBag.Title = "Status's";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1 class="text-center display-4 pb-3">@ViewBag.Title</h1>


<ol class="breadcrumb bg-faded">
    <li class="breadcrumb-item"><a href="~/Dashboard/Index" class="link-color">Dashboard</a></li>
    <li class="breadcrumb-item active">@ViewBag.Title</li>
</ol>

<div class="row">
    <div class="col-md-6">
        <form action="#" class="form-inline pb-3" method="get">
            <input class="form-control mr-sm-2" id="SearchString" name="SearchString" type="text" value="">
            <input class="btn btn-secondary" type="submit" value="Search">
        </form>
    </div>
    <div class="offset-md-4 col-md-2">
        <div class="float-create">
            @Html.ActionLink("Create New", "Create", null, new { @class = "btn btn-mint text-white" })
        </div>
    </div>
</div>

<table class="table table-bordered">
    <tr class="bg-inverse text-white">
        <th>
            @Html.DisplayNameFor(model => model.StatusName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.StatusTypeID)
        </th>
    </tr>

    @foreach (var item in Model)
    {
        <tr class="text-center clickable-row" id="@item.StatusID" data-href="/Details/@item.StatusID">
            <td>
                @Html.DisplayFor(modelItem => item.StatusName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StatusTypeID)
            </td>
        </tr>
    }
</table>

<div class="hidden-menu">
    <ul>
        <li><a href="/Edit/" class="btn btn-sm">Edit</a></li>
        <li><a href="/Delete/" class="btn btn-sm">Delete</a></li>
    </ul>
</div>
